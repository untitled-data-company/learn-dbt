# Source code

Application source for the Learn dbt site.

Planned structure:
- `components/` — UI components (editor, results table, story panel)
- `exercises/` — exercise definitions and graders
- `runner/` — SQL/dbt execution logic
- `content/` — chapter text and configuration
